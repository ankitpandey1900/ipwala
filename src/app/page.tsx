"use client";

import { ToolPageShell } from "@/components/layout/tool-page-shell";

// home page is just the shell with no prefilled command
export default function HomePage() {
  return <ToolPageShell />;
}
